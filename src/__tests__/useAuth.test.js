import React from 'react'

describe('useAuth test suite', () => {
  test('useAuth to generate token based on AuthProvider.init', () => {
    /*
      fetch test token from authprovider init

      render useAuth hook and test state jwtToken
    */
  })
})