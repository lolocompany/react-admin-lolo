import React from 'react'
import { describe } from 'yargs'

describe('useAdminContext test suite', () => {

  //DOM TEST
  test('render AdminContext component', () => {
    /*
      Render AdminContext component
      
      expect to be not null
    */
  })

  //DOM TEST
  test('useAdminContext to fetch list of selected accounts and set state accordingly', () => {
    /*
      create mock fetch data for accounts

      fetch jwtToken by rendering useAuth
      
      set accounts, and test accounts to be not null

      setSelectedAccount logic
    */
  })
})