import React from 'react'

describe('Filter test suite', () => {

  //DOM TEST
  test('test Filter rendering', () => {
    /*
      render Filter component

      expect to be not null
    */
  })

  //DOM TEST
  test('test Filter -> toInput function to render expected data', () => {
    /*
      create default test props schema to pass
      
      render toInput function ? / unit test expected output
    */
  })
})