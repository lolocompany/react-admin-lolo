import React from 'react'

describe('AppBarDropdown test suite', () => {
  
  //DOM TEST
  test('test accounts list to be displayed in DOM, based on useAdminContext', () => {

  })

  //DOM TEST
  test('test Change Account on user Interaction', () => {
    /*
      set sample data on accounts and selected account

      add userclick event to change account
    */
  })
})